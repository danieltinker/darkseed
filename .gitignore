node_modules/
dist/
.DS_Store
*.log
.cache/

# Generated data — kept out of git (regenerable via pnpm generate + pnpm migrate).
# Don't ignore the data/ dir wholesale because we need data/kb-seed/*.json tracked
# (source-of-truth templates). Ignore the GENERATED subpaths instead.
data/chains/
data/blobs/
data/reports/
data/*.sqlite*
data/index.json
dashboard/public/data/
synthetic_attack_chain_graphs_*.json

# Legacy pre-darkseed standalone dataset — not part of the joint architecture
legacy/

# Archived screenshots — keep current v3/sab only in screenshots/; archives off git
screenshots/archive/

# Build artifacts
dashboard/dist/
*.tsbuildinfo

# Playwright + test outputs
.playwright-mcp/
test-results/
playwright-report/

# Editor / OS
.idea/
.vscode/
Thumbs.db
